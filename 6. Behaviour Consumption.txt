projection;
strict ( 2 );
use draft;
define behavior for ZC_0226_CC alias CostCenter
use etag
{
  use create;
  use update;
  use delete;
  use action downloadTemplate;
  use action uploadExcel;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}